# Reuses the same structure as train_crop.py, just with configs/crop_nutrient.yaml
# Copy train_crop.py and replace --crop with --config, pointing to crop_nutrient.yaml.
# For brevity, just call:
# python scripts/train_crop.py --crop crop_nutrient   # if you extend the config detection