[build]
builder = "NIXPACKS"
nixpacksVersion = "1.28.0"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"

[buildArgs]
NIXPACKS_PYTHON_VERSION = "3.11"
