version: "3.8"
services:
  gaia:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - ../data:/app/data
      - ../checkpoints:/app/checkpoints
    environment:
      - STREAMLIT_SERVER_PORT=8501